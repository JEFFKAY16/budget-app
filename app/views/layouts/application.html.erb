<!DOCTYPE html>
<html>
  <head>
    <title>BudgetApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div class="container">
      <div class="nav menu">
        <div class="menu">
          <%= image_tag('menu-icon.png', :alt => 'menu', :width => '35px', :height => '35px') %>
        </div>
        <div class="nav-text">
          <h3>Budget<span>App</span></h3>
        </div>
        <div class="nav-link">
          <p><%= link_to "Sign in", new_user_session_path %></p>
        </div>
      </div>
      <%= yield %>
    </div>
  </body>
</html>
